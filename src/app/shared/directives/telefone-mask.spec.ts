import { TelefoneMask } from './telefone-mask';

describe('TelefoneMask', () => {
  it('should create an instance', () => {
    const directive = new TelefoneMask();
    expect(directive).toBeTruthy();
  });
});
